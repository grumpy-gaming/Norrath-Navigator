// preload.js
// This file runs in the renderer process *before* your HTML content loads.
// It's primarily used for securely exposing Node.js functionality to the renderer,
// but for now, it can remain empty.